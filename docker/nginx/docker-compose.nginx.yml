version: '2'
services:
    # Web Server NGinx
    web:
      image: nginx:alpine
      ports:
          - ${WEB_PORT}:80
      container_name: web
      volumes:
          - ./conf/nginx:/etc/nginx/conf.d
          - ${PROJECTS_PATH}:/var/www/html
      links:
          - php:php
