version: "3.6"
services:
  cache:
    image: redis:${CACHE_VERSION}
    ports:
      - ${CACHE_PORT}:6379
    container_name: cache
    links:
      - php:php
    networks:
      vfac:
        ipv4_address: ${CACHE_STATIC_IP}
